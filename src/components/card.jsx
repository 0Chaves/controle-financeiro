import { getTrancacoes, getTransacaoById, deleteTransacao } from "@/lib/transacaoDB";

export default function Card({id_transacao}){
    // const transacao = getTransacaoById(id_transacao)

    return(
        <div className="bg-gray-300 shadow-lg rounded-md">
            <p>qualquer coisa</p>
        </div>
    )
}